const Header = (props) => {
  return (
    <div className="block header">
      <h1>Node Headless Sample</h1>
      <p>
        Be sure to check out the console log
        <br />
        for more details about each interaction.
      </p>
    </div>
  );
};

export default Header;
